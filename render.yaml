services:
  - type: web
    name: AI-Powered-Disease-Recognition-10
    env: python
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y tesseract-ocr
      pip install -r requirements.txt
    startCommand: streamlit run home.py




